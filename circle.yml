machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install awscli parallel jq
  override:
    - docker info
    - docker build --rm=false -t reporter:latest .

test:
  override:
    - ./tests/circle.sh

deployment:
  prod:
    branch: master
    commands:
      - deploy/ecs.sh \
        --env prod \
        --region us-east-1 \
        --cpu-reservation 2048 \
        --mem-reservation 1024
      - deploy/dockerhub.sh

  dev:
    branch: dev
    commands:
      - deploy/ecs.sh \
        --env dev \
        --region us-east-1 \
        --cpu-reservation 1024 \
        --mem-reservation 512
